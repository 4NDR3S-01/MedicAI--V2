export {
  flushPendingProfileSync,
  getStoredSession,
  requestPasswordReset,
  signInWithEmail,
  signOut,
  signUpWithProfile,
  updatePassword,
  verifyEmailToken,
} from './auth.service';

export type { AppAuthSession } from './auth.service';
